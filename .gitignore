/_build
/cover
/deps
/doc
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Regenerated on every `mix ex_tauri.dev`. In dev it is a shell shim holding this
# machine's absolute project path; in a production build it is a Burrito binary.
/burrito_out
